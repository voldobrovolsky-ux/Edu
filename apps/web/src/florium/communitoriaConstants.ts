/** Макс. интервал между сообщениями одного автора (мс), при котором считаем «пачкой» — маленький вертикальный зазор. */
export const COMMUNITORIA_MESSAGE_TIGHT_GAP_MS = 5 * 60 * 1000;

/** После перехода на /florium модуль Communitoria фокусируется по флагу в sessionStorage. */
export const EDUMED_SESSION_FOCUS_COMMUNITORIA = "edumed:focusCommunitoria";
