/**
 * Единая шкала z-index (фиксированные значения для Tailwind: z-[N]).
 *
 * — Левый сайдбар: без z-index (как основной контент), чтобы портальные модалки (540+) перекрывали и контент, и сайдбар
 * — Контент / карточки: без явного z или 1–10
 * — Декор (предпросмотр персонализации, орб): 45 — выше фиксированных сайдбаров, ниже поповеров/модалок
 * — Локальные выпадашки / контекстные меню: 60–400
 * — Модалки и полноэкранные диалоги (в т.ч. через createPortal): 540+
 * — Вложенные модалки поверх: 560+
 * — Хаб / сплэш: 10000+
 */

export const ED_Z_PREVIEW_DECORATION = 45;
/** «Дух» Дедуса: глаз бродит по экрану, ниже панелей и модалок. */
export const ED_Z_DEDUS_SPIRIT = 28;
/** Поповеры трекера задач и аналогичные панели над контентом, под глобальными модалками. */
export const ED_Z_TRACKER_POPOVER = 400;
/** Помощник Дедус: выше обычного UI, ниже глобальных модалок и сплэша выхода. */
export const ED_Z_DEDUS_ASSISTANT = 530;
export const ED_Z_MODAL_GLOBAL = 540;
/** Второе модальное окно поверх основного (например дедлайн поверх карточки задачи). */
export const ED_Z_MODAL_STACK = 542;
/** Тосты напоминаний поверх модалок приложения. */
export const ED_Z_TOAST = 560;
