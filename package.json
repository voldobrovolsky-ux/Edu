{
  "name": "edumed",
  "version": "1.0.0",
  "private": true,
  "description": "EDUMED — цифровая экосистема управления учебным процессом (поставка v1.0.0).",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm --workspace apps/web run dev",
    "dev:api": "npm --workspace apps/api run start:dev",
    "build": "npm --workspace apps/web run build && npm --workspace apps/api run build"
  },
  "devDependencies": {
    "playwright": "^1.55.0"
  }
}
