{
  "name": "edumed-v0.1",
  "private": true,
  "version": "0.1.0",
  "description": "EDUMED школьная экосистема — monorepo (см. docs/)",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n web,api,payroll -c cyan,magenta,green \"npm run dev -w apps/web\" \"npm run dev -w apps/api\" \"npm run dev -w apps/payroll\"",
    "dev:web": "npm run dev -w apps/web",
    "dev:api": "npm run dev -w apps/api",
    "build": "npm run build -w apps/web && npm run build -w apps/api",
    "lint": "npm run lint -w apps/web --if-present && npm run lint -w apps/api --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
